name: Elixir Deep Checks

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  deep-static-checks:
    runs-on: ubuntu-latest
    env:
      MIX_ENV: dev
      DEV_DATABASE_URL: postgres://postgres:postgres@localhost:5432/vmemo_dev
      DEV_TYPESENSE_URL: http://localhost:8108
    strategy:
      matrix:
        otp: ["28"]
        elixir: ["1.19.5"]
    name: Deep Checks / Elixir ${{matrix.elixir}} Erlang/OTP ${{matrix.otp}}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: erlef/setup-beam@ee09b1e59bb240681c382eb1f0abc6a04af72764 # v1.23.0
        id: beam
        with:
          otp-version: ${{matrix.otp}}
          elixir-version: ${{matrix.elixir}}

      - name: Install dependencies
        run: mix deps.get

      - name: Compile
        run: mix compile --warnings-as-errors

      - name: Run Dialyzer
        run: mix dialyzer --format github

      - name: Dependency security audit
        run: mix hex.audit

      - name: Check unused deps locks
        run: mix deps.unlock --check-unused
