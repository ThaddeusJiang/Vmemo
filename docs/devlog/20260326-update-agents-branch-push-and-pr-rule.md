# 更新 AGENTS 分支 push 与 PR 规则

## 背景

- 现有项目规范已要求在干净工作区从新分支开始开发
- 需要补充非主分支的后续协作动作，避免遗漏 push 或 PR 创建

## 变更

- 在 `AGENTS.md` 的“项目规范”中新增两条：
  - 在非 `develop` 和 `main` 分支提交代码后，可以直接 push 到 remote
  - 在非 `develop` 和 `main` 分支上，如果当前分支还没有 PR，应创建对应 PR

## 结果

- 分支开发到提交流程更完整：提交后可直接 push，并明确要求及时创建 PR
