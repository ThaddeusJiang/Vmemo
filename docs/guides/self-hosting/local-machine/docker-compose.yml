services:
  vmemo:
    image: thaddeusjiang/vmemo
    restart: on-failure
    env_file:
      - .env
    ports:
      - "14000:4000"
    volumes:
      - ./vmemo_data/storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      typesense:
        condition: service_healthy
      moondream-station:
        condition: service_started

  postgres:
    image: postgres:18
    restart: on-failure
    hostname: postgres
    ports:
      - "15432:5432"
    volumes:
      - ./vmemo_data/pg-data:/var/lib/postgresql
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vmemo
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vmemo"]
      interval: 2s
      timeout: 5s
      retries: 20
      start_period: 5s

  typesense:
    image: typesense/typesense:30.1
    restart: on-failure
    hostname: typesense
    ports:
      - "18108:8108"
    volumes:
      - ./vmemo_data/ts-data:/data
    command: "--data-dir /data --api-key=${TYPESENSE_API_KEY:?TYPESENSE_API_KEY is required} --enable-cors"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -lc 'exec 3<>/dev/tcp/127.0.0.1/8108 && printf \"GET /health HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3 && grep -q \"200 OK\" <&3'",
        ]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s

  # Exposes Moondream Station bridge as an internal service for vmemo.
  moondream-station:
    image: alpine/socat
    restart: on-failure
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - "TCP-LISTEN:2020,fork,reuseaddr"
      - "TCP:host.docker.internal:2020"

  cloudflared:
    image: cloudflare/cloudflared:2026.3.0
    restart: unless-stopped
    depends_on:
      - vmemo
    environment:
      NO_PROXY: vmemo,localhost,127.0.0.1
      no_proxy: vmemo,localhost,127.0.0.1
    command: tunnel --no-autoupdate --url http://vmemo:4000 run --token ${CLOUDFLARED_TOKEN}
